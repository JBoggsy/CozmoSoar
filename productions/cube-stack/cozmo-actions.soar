sp {test-agent*apply*turn-in-place
    (state <s> ^operator <op>
              ^io.output-link <out>
             -^io.output-link.<old-act>.status running)
    (<op> ^name turn-in-place
          ^actions true
          ^angle <ang>
          ^speed <spd>)
-->
    (<out> ^turn-in-place <a>)
    (<a> ^angle <ang>
         ^speed <spd>)
}